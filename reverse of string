#include<stdio.h>
#include<string.h>
int main()
{
	int i,n;
	char str[30];
	printf("enter a reversed string");
	gets(str);
	n=strlen(str);
	printf("reversed string");
	for(i=n-1;i>=0;i--)
	{
		printf("%c",str[i]); 
	}
	return 0;
	
}
